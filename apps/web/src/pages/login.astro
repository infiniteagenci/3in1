---
// src/pages/login.astro

// Extract environment variable at build time/server-side
const { PUBLIC_WORKER_API_URL = 'http://localhost:8787' } = import.meta.env;

// Client-side functions will be defined in the script tag

---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Login - 3in1 Spiritual App</title>
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <style>
      .login-container {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        padding: 1rem;
      }
      
      .login-card {
        background: white;
        border-radius: 8px;
        padding: 2rem;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        width: 100%;
        max-width: 400px;
      }
      
      .google-btn {
        background-color: #4285f4;
        color: white;
        border: none;
        padding: 0.75rem 1rem;
        border-radius: 4px;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        font-size: 1rem;
        width: 100%;
      }

      .google-btn:hover {
        background-color: #3367d6;
      }

      .user-info {
        text-align: center;
      }

      .avatar {
        width: 80px;
        height: 80px;
        border-radius: 50%;
        object-fit: cover;
        margin-bottom: 1rem;
      }

      .loading {
        display: flex;
        align-items: center;
        justify-content: center;
        height: 100vh;
      }

      .hidden {
        display: none !important;
      }
    </style>
  </head>
  <body>
    <div className="login-container">
      <div id="login-content">
        <div className="loading" id="loading">Checking session...</div>
        <div className="login-card hidden" id="login-card">
          <h1>Meet Spirit - Your Spiritual Guide</h1>
          <p>Sign in with your Google account to begin your spiritual journey</p>
          <button id="google-login-btn" className="google-btn">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M22.56 12.25C22.56 11.47 22.49 10.72 22.36 10H12V14.26H17.92C17.66 15.63 16.88 16.79 15.71 17.57V20.34H19.28C21.36 18.42 22.56 15.6 22.56 12.25Z" fill="#4285F4"/>
              <path d="M12 23C14.97 23 17.46 22.02 19.28 20.34L15.71 17.57C14.73 18.23 13.48 18.64 12 18.64C9.14 18.64 6.73 16.69 5.85 14.09H2.19V16.91C4.04 20.53 7.72 23 12 23Z" fill="#34A853"/>
              <path d="M5.85 14.09C5.62 13.38 5.49 12.63 5.49 11.87C5.49 11.11 5.62 10.36 5.85 9.65V6.83H2.19C1.43 8.28 1 9.96 1 11.87C1 13.78 1.43 15.46 2.19 16.91L5.85 14.09Z" fill="#FBBC05"/>
              <path d="M12 4.76C13.92 4.76 15.61 5.46 16.94 6.83L20.36 3.84C18.22 1.82 15.26 0.5 12 0.5C7.72 0.5 4.04 2.97 2.19 6.59L5.85 9.65C6.73 6.95 9.14 4.76 12 4.76Z" fill="#EA4335"/>
            </svg>
            Sign in with Google
          </button>
        </div>
        <div className="login-card hidden" id="user-card">
          <div className="user-info">
            <img id="avatar" src="" alt="Avatar" className="avatar" />
            <h2 id="welcome-text">Welcome</h2>
            <p id="user-email"></p>
            <button id="logout-btn" className="google-btn">Logout</button>
          </div>
        </div>
      </div>
    </div>
    
    <script define:vars={{PUBLIC_WORKER_API_URL: import.meta.env.PUBLIC_WORKER_API_URL || 'http://localhost:8787'}}>
      // DOM elements
      const loadingEl = document.getElementById('loading');
      const loginCardEl = document.getElementById('login-card');
      const userCardEl = document.getElementById('user-card');
      const googleLoginBtn = document.getElementById('google-login-btn');
      const logoutBtn = document.getElementById('logout-btn');
      const avatarEl = document.getElementById('avatar');
      const welcomeTextEl = document.getElementById('welcome-text');
      const userEmailEl = document.getElementById('user-email');

      // Function to add hidden class
      function hideElement(el) {
        el.classList.add('hidden');
      }

      // Function to remove hidden class
      function showElement(el) {
        el.classList.remove('hidden');
      }

      // Function to check session status
      async function checkSession() {
        try {
          const token = localStorage.getItem('session_token');
          if (!token) {
            // Show login card
            hideElement(loadingEl);
            showElement(loginCardEl);
            hideElement(userCardEl);
            return;
          }

          const response = await fetch(PUBLIC_WORKER_API_URL + '/auth/validate', {
            headers: {
              'Authorization': 'Bearer ' + token
            }
          });

          if (response.ok) {
            const { user } = await response.json();

            // Update user info in UI
            if (user.avatar_url) {
              avatarEl.src = user.avatar_url;
              avatarEl.style.display = 'block';
            } else {
              avatarEl.style.display = 'none';
            }

            welcomeTextEl.textContent = `Welcome, ${user.name}`;
            userEmailEl.textContent = user.email;

            // Show user card
            hideElement(loadingEl);
            hideElement(loginCardEl);
            showElement(userCardEl);
          } else {
            // Session invalid, clear token and show login
            localStorage.removeItem('session_token');
            hideElement(loadingEl);
            showElement(loginCardEl);
            hideElement(userCardEl);
          }
        } catch (error) {
          console.error('Error checking session:', error);
          // On error, show login screen
          localStorage.removeItem('session_token');
          hideElement(loadingEl);
          showElement(loginCardEl);
          hideElement(userCardEl);
        }
      }

      // Function to initiate Google login
      async function initiateGoogleLogin() {
        try {
          console.log('=== Frontend Google OAuth Debug ===');
          console.log('API URL:', PUBLIC_WORKER_API_URL);
          console.log('Requesting OAuth URL from:', PUBLIC_WORKER_API_URL + '/auth/google/url');
          console.log('==================================');

          const response = await fetch(PUBLIC_WORKER_API_URL + '/auth/google/url');
          const { url } = await response.json();

          console.log('Received OAuth URL:', url);

          // Redirect to Google OAuth URL
          window.location.href = url;
        } catch (error) {
          console.error('Error initiating Google login:', error);
        }
      }

      // Function to handle logout
      async function logout() {
        try {
          const token = localStorage.getItem('session_token');
          if (token) {
            await fetch(PUBLIC_WORKER_API_URL + '/auth/logout', {
              method: 'POST',
              headers: {
                'Authorization': 'Bearer ' + token,
                'Content-Type': 'application/json'
              }
            });
          }

          localStorage.removeItem('session_token');
          // Show login card after logout
          hideElement(loadingEl);
          showElement(loginCardEl);
          hideElement(userCardEl);
        } catch (error) {
          console.error('Error logging out:', error);
        }
      }

      // Add event listeners
      googleLoginBtn.addEventListener('click', initiateGoogleLogin);
      logoutBtn.addEventListener('click', logout);

      // Check session on page load
      checkSession();

      // Check for OAuth callback in URL
      if (window.location.search.includes('code=')) {
        const urlParams = new URLSearchParams(window.location.search);
        const code = urlParams.get('code');

        if (code) {
          // Show loading while processing
          showElement(loadingEl);
          hideElement(loginCardEl);
          hideElement(userCardEl);

          // Send code to our backend to exchange for token
          fetch(PUBLIC_WORKER_API_URL + '/auth/google/callback', {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json'
            },
            body: JSON.stringify({ code })
          })
          .then(response => response.json())
          .then(data => {
            if (data.session_token) {
              // Store the session token
              localStorage.setItem('session_token', data.session_token);

              // Check session again to update UI
              checkSession();
            } else {
              console.error('No session token received');
              // Show login screen again
              hideElement(loadingEl);
              showElement(loginCardEl);
              hideElement(userCardEl);
            }
          })
          .catch(error => {
            console.error('Error exchanging code:', error);
            // Show login screen again
            hideElement(loadingEl);
            showElement(loginCardEl);
            hideElement(userCardEl);
          });
        }
      }
    </script>
  </body>
</html>